此資料夾用來放置你的相片檔案（供相簿使用）。

建議做法：

- 把照片放在此資料夾，檔名例如：`tree-1.jpg`, `tree-2.jpg`。
- 在 `index.html` 的相簿裝飾按鈕上，將 `data-photo` 屬性改為相對路徑，例如：
  `data-photo="assets/photos/tree-1.jpg"`。
- 若要一次上傳多張照片，請維持適當尺寸（例如 1200×800 或 800×533），以利載入速度。

如果你要，我可以：
- 幫你把 `index.html` 中的 `data-photo` 從外部圖片替換成你放入此資料夾的檔名（請先把檔案上傳到此資料夾）。
- 或者加一個上傳介面（需要後端支援或第三方服務）。
